// 1. if 조건문
if(조건이 참이면){
  // 코드 실행
}
else if(조건이 참이면){
  // 코드 실행
}
else{
  // 위 조건이 모두 거짓이면 코드 실행
}

// Switch 조건문
switch(조건){
  case 값1:
    // 조건이 '값1'일 때 실행
    break
  case 값2:
    // 조건이 '값2'일 때 실행
    break
  default:
    // 조건이 위 값이 아닐 때 실행
  }

// For 반복문
for(초기화; 조건; 증감){
  // 반복 실행할 코드
}

// For of 반복문
for(let 변수 of 배열){
  // 반복 실행할 코드
}

// For in 반복문
for(let key in 객체){
  // 반복 실행할 코드
}

// While 반복문
while(조건){
  // 조건이 참이면 무한 반복
  // 거짓값이 있어야 함
}